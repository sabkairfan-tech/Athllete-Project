const express = require("express");
const {getallusers,postusers} = require("../Controller/UserController");
const router = express.Router();




router.get("/users", getallusers);
router.post("/create", postusers);

module.exports = router;